import { Meta } from "@storybook/react";
import { Select } from "./select";

const meta: Meta = {
	title: "Inputs/Select",
};

export default meta;

export const Default = () => {
	return <Select />;
};
